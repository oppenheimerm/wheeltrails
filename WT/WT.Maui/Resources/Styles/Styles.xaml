<?xml version="1.0" encoding="UTF-8" ?>
<?xaml-comp compile="true" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:WT.Maui.Converters">

    <!-- Converters -->
    <converters:StringNotNullOrEmptyConverter x:Key="StringNotNullOrEmptyConverter" />
    <converters:BoolToSubmittingTextConverter x:Key="BoolToSubmittingTextConverter" />
    <converters:InverseBoolConverter x:Key="InverseBoolConverter" />

    <!-- Typography styles (Material 3 inspired) -->
    <Style x:Key="HeadlineLarge" TargetType="Label">
        <Setter Property="FontFamily" Value="FigtreeBold" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="TextColor" Value="{DynamicResource OnSurface}" />
    </Style>

    <Style x:Key="HeadlineMedium" TargetType="Label">
        <Setter Property="FontFamily" Value="FigtreeSemiBold" />
        <Setter Property="FontSize" Value="18" />
        <Setter Property="TextColor" Value="{DynamicResource OnSurface}" />
    </Style>

    <Style x:Key="Body" TargetType="Label">
        <Setter Property="FontFamily" Value="FigtreeRegular" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="TextColor" Value="{DynamicResource OnSurface}" />
    </Style>

    <Style x:Key="FormLabel" TargetType="Label" BasedOn="{StaticResource Body}">
        <Setter Property="FontFamily" Value="FigtreeMedium" />
        <Setter Property="FontSize" Value="14" />
    </Style>

    <!-- Card / Border style used for containers -->
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceContainer}" />
        <Setter Property="Stroke" Value="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}" />
        <Setter Property="StrokeShape" Value="RoundRectangle 12" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="Margin" Value="0" />
    </Style>

    <!-- Top AppBar style (ContentView wrapper) -->
    <Style x:Key="TopAppBar" TargetType="ContentView">
        <Setter Property="BackgroundColor" Value="{StaticResource SurfaceContainer}" />
        <Setter Property="HeightRequest" Value="{StaticResource TopAppBarHeight}" />
        <Setter Property="Padding" Value="12,8" />
    </Style>

    <!-- Buttons -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <!-- Use Brush-backed Background and DynamicResource so theme changes update at runtime -->
        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
        <Setter Property="TextColor" Value="{DynamicResource OnPrimaryBrush}" />
        <Setter Property="FontFamily" Value="FigtreeSemiBold" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="HeightRequest" Value="44" />
        <Setter Property="Padding" Value="12,8" />
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource SecondaryBrush}" />
        <!-- Ensure sufficient contrast for secondary buttons using theme-aware on-secondary colors -->
        <Setter Property="TextColor" Value="{DynamicResource OnSecondaryBrush}" />
        <Setter Property="FontFamily" Value="FigtreeRegular" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="HeightRequest" Value="44" />
    </Style>

    <Style x:Key="DangerButton" TargetType="Button">
        <!-- ErrorBrush is a SolidColorBrush; keep TextColor white for contrast -->
        <Setter Property="Background" Value="{DynamicResource ErrorBrush}" />
        <Setter Property="TextColor" Value="{DynamicResource OnErrorContainer}" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="HeightRequest" Value="44" />
    </Style>

    <!-- Material 3: Filled and Outlined button variants (reusable) -->
    <Style x:Key="FilledButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <!-- Inherit PrimaryButton (filled) so no extra setters required -->
    </Style>

    <Style x:Key="OutlinedButton" TargetType="Button">
        <!-- Transparent background, primary colored stroke and text -->
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="TextColor"
                Value="{DynamicResource PrimaryBrush}" />
        <Setter Property="BorderColor"
                Value="{DynamicResource PrimaryBrush}" />
        <Setter Property="BorderWidth" Value="2" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="FontFamily" Value="FigtreeSemiBold" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="HeightRequest" Value="44" />
        <Setter Property="Padding" Value="12,8" />
    </Style>

    <!-- Entry / Editor (form inputs)
         NOTE: Entry and Editor do not expose a Padding property in MAUI XAML.
         Use Margin or HeightRequest to influence layout; internal text padding is platform-specific.
    -->
    <Style x:Key="FormEntry" TargetType="Entry">
        <Setter Property="FontFamily" Value="FigtreeRegular" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray800}}" />
        <Setter Property="HeightRequest" Value="44" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="TextColor" Value="{DynamicResource OnSurface}" />
    </Style>

    <Style x:Key="FormEditor" TargetType="Editor">
        <Setter Property="FontFamily" Value="FigtreeRegular" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray800}}" />
        <Setter Property="HeightRequest" Value="100" />
        <Setter Property="TextColor" Value="{DynamicResource OnSurface}" />
    </Style>

    <!-- Small UI helpers -->
    <Style x:Key="MutedLabel" TargetType="Label" BasedOn="{StaticResource Body}">
        <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
    </Style>

    <!-- Banner styles -->
    <Style x:Key="PrimaryBanner" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PrimaryContainer}" />
        <Setter Property="Stroke" Value="Transparent" />
        <Setter Property="StrokeShape" Value="RoundRectangle 24" />
        <Setter Property="Padding" Value="12" />
    </Style>

    <Style x:Key="ErrorBanner" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ErrorContainer}" />
        <Setter Property="Stroke" Value="Transparent" />
        <Setter Property="StrokeShape" Value="RoundRectangle 8" />
        <Setter Property="Padding" Value="10" />
    </Style>

    <!-- ActivityIndicator default color -->
    <Style TargetType="ActivityIndicator">
        <Setter Property="Color" Value="{AppThemeBinding Light={StaticResource PrimaryLight}, Dark={StaticResource White}}" />
    </Style>

    <!-- Page default -->
    <Style TargetType="Page" ApplyToDerivedTypes="True">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource Background}, Dark={StaticResource SurfaceContainerDark}}" />
    </Style>

    <!-- Explicit ContentPage style to ensure ContentPage-based pages inherit the same background behavior -->
    <Style TargetType="ContentPage" ApplyToDerivedTypes="True">
        <Setter Property="Padding" Value="0" />
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource Background}, Dark={StaticResource SurfaceContainerDark}}" />
    </Style>

    <Style TargetType="Shell" ApplyToDerivedTypes="True">
        <Setter Property="Shell.BackgroundColor" Value="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}" />
        <Setter Property="Shell.ForegroundColor" Value="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource SecondaryDarkText}}" />
        <Setter Property="Shell.TitleColor" Value="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource SecondaryDarkText}}" />
        <Setter Property="Shell.DisabledColor" Value="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray950}}" />
        <Setter Property="Shell.UnselectedColor" Value="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray200}}" />
        <Setter Property="Shell.NavBarHasShadow" Value="False" />
        <Setter Property="Shell.TabBarBackgroundColor" Value="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Black}}" />
        <Setter Property="Shell.TabBarForegroundColor" Value="{AppThemeBinding Light={StaticResource Magenta}, Dark={StaticResource White}}" />
        <Setter Property="Shell.TabBarTitleColor" Value="{AppThemeBinding Light={StaticResource Magenta}, Dark={StaticResource White}}" />
        <Setter Property="Shell.TabBarUnselectedColor" Value="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource Gray200}}" />
    </Style>

</ResourceDictionary>
